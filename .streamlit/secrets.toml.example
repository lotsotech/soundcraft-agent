# Copy this to .streamlit/secrets.toml for local use (already in .gitignore)
# On Streamlit Cloud, add these via the Secrets panel in your app settings

GOOGLE_API_KEY   = "your_google_api_key_here"
PINECONE_API_KEY = "your_pinecone_api_key_here"  # optional — enables semantic search
